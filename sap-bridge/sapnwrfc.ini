# SAP NW RFC Connection Configuration
# Copy to secrets/ for production, never commit with real values
[DEFAULT]
ashost = ${SAP_ASHOST}
sysnr = ${SAP_SYSNR}
client = ${SAP_CLIENT}
user = ${SAP_USER}
passwd = __DOCKER_SECRET__  ; Replaced at runtime via /run/secrets/sap_password
lang = ${SAP_LANG}
