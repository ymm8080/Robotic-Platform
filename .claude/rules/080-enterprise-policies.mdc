---
description: 人工通知优先级、审批矩阵与安全合规策略
globs: **/notifications/**, **/compliance/**
alwaysApply: true
---

# 企业级策略：通知矩阵 + 安全合规

## 1. 通知优先级矩阵
| 级别 | 延迟要求 | 渠道 | 聚合窗口 |
|------|----------|------|----------|
| P0（系统宕机/数据丢失） | 立即 | MQTT → 飞书 → 短信 → 电话（4层降解） | 不聚合，逐条发送 |
| P1（功能降级/偏差告警） | 5 分钟内 | MQTT → 飞书 | 5 分钟滑动窗口聚合 |
| P2（日常事件/状态变更） | 10 分钟内 | 飞书（静默通知） | 10 分钟滑动窗口聚合 |

## 2. 渠道降解链
- P0：飞书 → 短信 → 电话（每层失效自动降级到下一层）
- P1：飞书（不可达则缓存，恢复后补发）
- P2：飞书静默推送（不触发客户端弹窗）

## 3. 审批双通道
- 人工审批必须同时支持飞书和 WeCom（企业微信）两个渠道
- 两个渠道独立送达，以先到的响应为准（乐观锁）
- 审批结果通过 Redis 分布式锁防止重复处理

## 4. 短信频率控制
- 同一原因 30 分钟内最多 1 条短信
- 每日所有原因累计最多 100 条短信
- 超限后转为飞书通知 + 日志记录，第二天重置

## 5. 安全合规强制项
- 密码复杂度：最小 12 字符，含大写/小写/数字/特殊字符各至少 1
- 所有管理接口必须 2FA（口令 + TOTP 或硬件 Key）
- 审计日志保留 180 天（UTC 时间戳），禁止删除/修改
- 数据库只允许内部网络访问（禁止暴露到 Docker host 网络）
- Dify 必须配置离线模式（禁止 AI 数据出境到非授权地域）
