## Changes

- **worm_blackbox.py**: Add `threading.Lock` to `write()` to prevent rotate/persist race condition under concurrent access
- **mqtt_publisher.py**: Remove dot from topic component regex to prevent MQTT wildcard ambiguity
- **task_allocator.py**: Rename `_model_of` to `model_of` (public API for lane model matching)
- **test_v4_adjustments.py / test_scheduling_and_safety.py / test_platform_adapter_survival.py**: Remove `sys.path.insert` hack (`pyproject.toml` already configures `pythonpath = ["."]`)
- **fixed_lane_map.py**: Add path cache for unfiltered `shortest_path` queries with invalidation on `add_lane`/`block_lane`/`unblock_lane`

## Verification

- All 61 core tests pass (2 skipped)
- Ruff I001 import sorting clean on modified files
