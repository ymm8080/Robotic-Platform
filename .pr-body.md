## Summary

Completes Phase 0 DoD on the **SAP side**, on top of the merged #73. PR #73 landed
only the shared registry + DISPATCH-side wiring (its title was updated mid-flight but
the SAP-side commit did not reach the merge). This PR brings the SAP strategy layer to
the same single-source-of-truth as DISPATCH.

Plan reference: `项目实施计划_v7.0_20260714.md` §Phase 0 — "双策略层品牌知识去重".
Design rationale: `01_architecture/PHASE0_DUAL_STRATEGY_FINDING.md`.

## What changed (SAP side only — DISPATCH was merged in #73)

- `sap-bridge/strategies/base.py` — new `@staticmethod _shared_supported_versions(brand_key)`
  (lazy-imports `get_brand_knowledge`, mirroring base.py's existing lazy `core.messages`
  convention — cycle-free).
- `sap-bridge/strategies/{mir,otto,kuka,geekplus,hairobotics,quicktron}.py` — each
  `supported_versions` @property delegates to the helper with a literal brand key
  (mirrors the DISPATCH-side `_capability_from_knowledge` literal-key pattern).

7 files, +20/−7. No other strategy behavior touched.

## Behavior preservation

- `supported_versions` verified **identical** to the prior hardcoded lists for all six
  brands (mir `["1.1.0"]`, otto/kuka/hairobotics `["2.0.0"]`, geekplus/quicktron
  `["1.1.0","2.0.0"]`) — parity script confirms SAP == shared registry for all 6.
- `normalize_battery` / `get_quirks` / `dispatch` / state handlers left untouched
  (legitimately separate runtime behaviors per the finding doc).

## DoD status — ✅ met (now both sides)

v7.0 plan Phase 0 DoD: *"共享品牌知识模块被两侧引用（grep 可证）"*.

- ✅ DISPATCH side (core) — landed in #73 (`core/adapter/brands/strategies.py`)
- ✅ SAP side (sap-bridge) — this PR (`sap-bridge/strategies/base.py` + 6 brand files)

## Verification (re-run on this branch, not assumed)

- `ruff check sap-bridge/strategies/ --config sap-bridge/ruff.toml` — All checks passed
- `ruff format --check` — 9 files already formatted
- `pytest sap-bridge/tests/{test_strategies,test_geekplus_strategy,test_hairobotics_strategy,test_quicktron_strategy}.py` — **128 passed**
- Parity: all six brands SAP `supported_versions` == shared registry — OK
- Both-sides grep: DISPATCH (master) + SAP (this branch) confirmed

Note: the full `pytest sap-bridge/tests/` suite reports DB connection failures
(`psycopg2.OperationalError: Connection refused`, PostgreSQL not running locally) —
pre-existing infrastructure, unrelated to a `supported_versions` property wiring.

## Process note

The SAP-side code is byte-identical to the commit that was already run through the
`auto-implement-and-PR.sh` STEP 3 multi-agent Workflow (adversarial reviewer + verify
agent, `all_pass=true`) in the prior session. It is cherry-picked here onto current
master unchanged, so that review still applies; the STEP 4 verify matrix above was
re-run fresh on this branch.

## Notes for review

- Do not merge via this PR — the AI code review gate handles merge.
- Future: wire `battery_quirks` / `field_mapping` / `error_quirks` and validate against
  both layers; resolve `[GUESS]` Quicktron voltages.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
