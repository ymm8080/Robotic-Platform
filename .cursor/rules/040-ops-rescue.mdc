---
description: 凌晨2点急救与运维排障规则
globs: ["**/*.sh", "**/crontab", "**/rsync*"]
---

# 运维排障约束

1. **安全模式优先**：异常时第一步访问 `/api/safe-mode` 止血，绝不删数据。
2. **恐慌模式触发词库**：
   - 崩了/卡了/全红了/挂了/死了
   - 不动了/没反应/打不开/连不上
   - 没消息/没回传/乱套了/出事了
   - 怎么办/急/救命/完了
   - 遇到以上词汇，只输出 safe-mode 命令和看日志命令，绝不给修改代码的建议。
3. **补录乐观锁**：离线补录前必须切断 SAP 新单推送，补录 API 必须带 `expected_qty` 做乐观锁校验，防库存打架。
4. **降级演练**：上线前3/7/14天强制演练，每月盲演，30分钟内吞吐量必须≥50%。
