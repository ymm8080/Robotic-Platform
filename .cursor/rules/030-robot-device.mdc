---
description: 多品牌机器人 API 适配与物理世界交互规则
globs: ["**/robots/**/*.py", "**/mqtt*", "**/subflows/**"]
---

# 机器人适配层约束

1. **统一入参**：统一映射为 `{robot_id, action, target, payload}`。
2. **电量与宽限期**：电量 <20% 只派短单；心跳丢失 120s 才 offline (防 WiFi 漫游假死)。
3. **环境染色**：心跳必须带 `env_tag` (PROD/STAGING)，不匹配直接丢弃并告警，防测试车连生产。
4. **位移与物理死锁**：EXECUTING 状态 10 秒 position 未变化判定卡死，发后退指令前必须检查急停状态。
5. **人机物理安全**：`HUMAN_TASK` 不参与机器人资源锁和区域锁。
6. **厂商 API 偏差**：遇到 200 OK 但不动的情况，记录 `api_deviation_log` 表作为甩锅证据。
7. **厂商 API 升级管控**：厂商 API 升级时，必须通过 `brand_config` 表的 `api_version` 字段切换，新旧版本并行运行 48 小时，确认无偏差后再下线旧版本。
