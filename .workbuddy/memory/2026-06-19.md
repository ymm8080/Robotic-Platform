# 2026-06-19

## WorkBuddy 规则配置：全部技能设为 auto-apply

### 背景
用户要求将 Qoder、Cursor、Cursor Extension 中所有技能的 .mdc 规则文件统一设置为 `alwaysApply: true`（编码时自动应用）。

### 修改内容

**修复了 13 个缺失 `alwaysApply: true` 的 .mdc 文件：**

| 文件 | 位置 | 原因 |
|------|------|------|
| 020-sap-bridge.mdc | .cursor/rules/ + .qoder/rules/ | 缺少 alwaysApply 字段 |
| 030-robot-device.mdc | .cursor/rules/ + .qoder/rules/ | 缺少 alwaysApply 字段 |
| 040-ops-rescue.mdc | .cursor/rules/ + .qoder/rules/ | 缺少 alwaysApply 字段 |
| 050-state-machine.mdc | .cursor/rules/ + .qoder/rules/ | 缺少 alwaysApply 字段 |
| 060-db-and-outbox.mdc | .cursor/rules/ + .qoder/rules/ | 缺少 alwaysApply 字段 |
| 070-infra-and-rescue.mdc | .cursor/rules/ + .qoder/rules/ | 缺少 alwaysApply 字段 |
| state-machine-patterns.mdc | .qoder/rules/ | 原为 `"alwaysApply": false` → 改为 `true` |

### 验证结果
- 总计 225 个 .mdc 文件（112 .cursor + 113 .qoder）
- 全部包含 `alwaysApply: true`，0 个遗漏
- 检查了 .cursor-only 的 12 个额外规则文件，均已正确配置

## 生成 Skills 库存 Excel

用户要求列出所有技能到 Excel 表格。生成了 `AI_Skills_Inventory.xlsx`：

### 文件内容
- **Sheet 1 - Master List**: 全部 465 条记录（含技能 + 规则），带颜色区分和筛选
- **Sheet 2 - Skills Only**: 240 个技能条目（120 cursor + 120 qoder）
- **Sheet 3 - Rules Only**: 225 个 .mdc 规则条目（118 cursor + 107 qoder），自动高亮非 auto-apply 的规则
- **Sheet 4 - Summary**: 汇总统计

## 还原所有 .mdc alwaysApply 为默认值

用户要求将所有技能的 `alwaysApply` 恢复为默认值。

### 修改
- **224 个 YAML 格式 .mdc** → 移除 `alwaysApply` 字段（恢复为上下文匹配默认行为）
- **1 个 JSON 格式 .mdc** (state-machine-patterns.mdc) → `"alwaysApply": false`

### 验证
- 0 个文件残留 `alwaysApply`（YAML 格式全部清除）
- 1 个 JSON 文件保留 `"alwaysApply": false`（符合默认值）

## 生成 Skills 激活方式 Excel

生成 `AI_Skills_Activation.xlsx`，列出每个技能的 `alwaysApply` 值 + 激活方式。

### 列
- **alwaysApply**: true/false/(absent)/(no rule)
- **Activation**: Context-based / Manual / Auto-apply
- **How to Activate**: 具体激活操作方法
- **Globs**: 上下文自动触发的文件模式

### 汇总
| 类型 | 数量 |
|------|------|
| Context-based (有 .mdc 规则) | 98 |
| Manual (无 .mdc 规则) | 22 |
| Auto-apply | 0 |

## 修复 karpathy-guidelines 不对称 + 更新 Excel

### 修复内容
- 新建 `.cursor/skills/karpathy-guidelines/SKILL.md`（12 条 Karpathy 编码准则）
- 新建 `.qoder/skills/karpathy-guidelines/SKILL.md`（同步，hash 一致）
- 新建 `.qoder/rules/karpathy-guidelines.mdc`（与 .cursor 规则同步）
- 验证：4 个文件均在位，SKILL.md hash 一致

### Excel 更新（两个文件已重新生成）
| 文件 | 更新前 | 更新后 | 变化 |
|------|--------|--------|------|
| AI_Skills_Inventory.xlsx | 465 条 | **468 条** | +3（karpathy 的 2 SKILL.md + 1 .mdc） |
| AI_Skills_Activation.xlsx | 120 skills | **121 skills** | +1（karpathy-guidelines 加入） |
| Context-based 激活 | 98 | **99** | +1 |
| Manual（无规则） | 22 | **22** | 不变 |

## ⚡ 全量规则清理：13 保留 + 其余全部删除

按用户制定的筛选逻辑执行全量清理。

### 筛选标准
只保留"AI 在写代码时必须被动遵守，否则会产生编译错误、安全漏洞或物理设备事故"的规则。操作手册、分析工具、通用教程一律不留。

### 保留的 13 个 .mdc 规则

| # | 文件 | 激活 | globs 绑定 | 理由 |
|---|------|------|-----------|------|
| 1 | 000-global-iron-rules | alwaysApply:true | (全局) | 项目宪法 + 3 条灾备铁律注入 |
| 2 | karpathy-guidelines | (上下文) | **/* | AI 防呆指南 |
| 3 | 010-nodered-core | (上下文) | flows/**/*.json, nodes/**/*.js | Node-RED 流语法极敏感 |
| 4 | 020-sap-bridge | (上下文) | sap-bridge/**/*.py, .js | SAP RFC/BAPI 调用规范 |
| 5 | 030-robot-device | (上下文) | robot/**/*.py, agv/**/*.cpp | AGV 驱动参数严格受控 |
| 6 | 040-ops-rescue | (上下文) | ops/**/*.py, scripts/**/*.sh | 操作脚本回滚/锁机制 |
| 7 | 050-state-machine | (上下文) | state_machine/**/*.py | 状态流转错误→物理碰撞 |
| 8 | 060-db-and-outbox | (上下文) | **/*.sql, **/models/*.py | 发件箱模式/事务隔离 |
| 9 | 070-infra-and-rescue | (上下文) | docker-compose*.yml, k8s/** | 容器编排/紧急救援 |
| 10 | circuit-breaker | (上下文) | sap-bridge/**/*.py, http_clients/*.py | 外部 API 级联故障防护 |
| 11 | idempotency-patterns | (上下文) | **/handlers/**/*.py, mqtt/**/*.py | MQTT 重复派单防护 |
| 12 | mqtt-protocol | (上下文) | mqtt/**/*.py, robot/agv/**/*.py | AGV Topic/Payload 规范 |
| 13 | observability | (上下文) | **/logging/**, **/metrics/** | trace_id/span_id 强制记录 |

### 删除统计
| 类别 | 删除数量 |
|------|---------|
| .mdc 规则文件（.cursor） | 101 |
| .mdc 规则文件（.qoder） | 101 |
| **合计** | **202** |

### 删除的规则类别
- ❌ 添加类手册（adding-* 等 8 个）：操作教程
- ❌ 前端/UI（accessibility, css 等）：机器人大后端不需要
- ❌ 工具执行器（agent-browser, tmux, pdf 等）：动作调用
- ❌ 排查/教程（incident-response, chaos, debugging 等）：过程式指南
- ❌ Git/CI（creating-pr, setting-up-ci 等）：与核心业务无关
- ❌ OTEL 全家桶（otel-collector 等 4 个）：监控配置手册
- ❌ 二选一落选（circuit-breaker-pattern/setup, idempotency-handling, mqtt-development）
- ❌ 无实质内容（api-security-testing, database-design, rest-api-design 等）
- ❌ 无用类别（parallel-*, react-native, seo 等）

### 关键操作
- **disaster-recovery**: .mdc 转为 SKILL.md（按需加载），3 条灾备铁律合并进全局规则
- **000-global-iron-rules**: 新增第 10~12 条（持久化/备份/回滚铁律）
- **4 个缺失的 .qoder 规则**: circuit-breaker, idempotency-patterns, mqtt-protocol, observability → 从 .cursor 同步创建

### 纠正：SKILL.md 恢复
误删了 SKILL.md 技能文件。从 `skills-lock.json` 记录的 11 个 GitHub 源仓库重新下载并恢复：
- 80 个技能目录 → `.agents/skills/`（GitHub 源）
- 复制到 `.cursor/skills/` 和 `.qoder/skills/`
- 恢复后结构：95 子目录 + 24 个独立 .md 文件（两边基本对称）

## 最终同步：所有规则转为技能 + 全量安装 + Excel 最终版

### 操作步骤
1. **旧 Excel 226 规则 → 全部检查**：13 个 keepers 保留为 .mdc，其余 213 个已全部有对应 SKILL.md（0 遗漏）
2. **补全 3 个缺失**：api-security-testing, load-testing, chaos-engineering 创建 SKILL.md
3. **全量同步到 3 个位置**：.cursor (127), .qoder (127), cursor-ext (144)
4. **生成最终 Excel**：`Skills_Inventory_2026-06-19.xlsx` + `Skills_Activation_2026-06-19.xlsx`

### 最终状态
| 位置 | 技能 | 规则 | 说明 |
|------|------|------|------|
| `.cursor` | 127 | 13 | 含 22 个独立 .md + 105 个 SKILL.md 目录 |
| `.qoder` | 127 | 14 | 同步 .cursor，额外 state-machine-patterns.json |
| `cursor-ext` | 144 | — | 全局技能，覆盖所有 Cursor 项目 |
| **总计** | **398** | **27** | 去重后 146 个唯一技能 |

### 激活方式
- `alwaysApply: true`：000-global-iron-rules + karpathy-guidelines
- Context-based：circuit-breaker, idempotency-patterns, mqtt-protocol, observability 等（绑定 globs）
- Manual：其余 130+ 按需技能（prompt 中引用名称）
